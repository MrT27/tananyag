cd projektmappa
git init
git add index.html
git commit -m "Első verzió"
git log

# Korábbi commit megtekintése
git checkout COMMIT_AZONOSITO

# Visszatérés a legfrissebb állapothoz
git checkout main

# Régebbi repository esetén az ág neve lehet:
git checkout master
